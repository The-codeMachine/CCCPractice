/*#include <iostream>

int main() {
	size_t startingSize;
	std::cin >> startingSize;

	size_t yobiSize = 0;	
	while (true) {
		std::cin >> yobiSize;

		if (startingSize <= yobiSize)
			break;

		startingSize += yobiSize;
	}	

	std::cout << startingSize << "\n";

	return 0;
} */
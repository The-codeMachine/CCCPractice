/*#include <iostream>

int main() {
	int start;
	size_t num;

	std::cin >> start;
	std::cin >> num;

	int end = start;
	for (size_t i = 0; i < num; ++i) {
		char add;
		int amount;
		
		std::cin >> add;
		std::cin >> amount;

		if (add == '+')
			end += amount;
		else if (add == '-')
			end -= amount;
	}

	std::cout << end << "\n";

	return 0;
} */